# Ultralytics YOLO 🚀, AGPL-3.0 license
# Custom dataset for Ultralytics
# Documentation: https://docs.ultralytics.com/datasets/detect/coco8/
# Example usage: yolo train data=coco8.yaml
# parent
# ├── ultralytics
# └── yolo_varejo 

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /home/rodrigocm/datasets/opticDiscDetection # dataset root dir
train: baseline1/train/images # train images (relative to 'path')
val: baseline1/val/images # val images (relative to 'path') 
test: baseline1/test/images # test images (optional)

nc: 1

names: ['optic_disc']